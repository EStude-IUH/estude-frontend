"use client";

import { ArrowLeft, Save } from "lucide-react";
import { useParams, useRouter } from "next/navigation";
import { useEffect, useState } from "react";
import { AssessmentShell, ErrorPanel, LoadingPanel, PageHeading } from "@/components/assessment/assessment-shell";
import { Button } from "@/components/ui/button";
import { academicDataService, questionBankService } from "@/lib/assessment-api";
import type { Difficulty, QuestionInput, QuestionOption, QuestionType, Subject, Topic } from "@/types/assessment";

const initialOptions: QuestionOption[] = [{ id: "a", label: "A", text: "" }, { id: "b", label: "B", text: "" }, { id: "c", label: "C", text: "" }, { id: "d", label: "D", text: "" }];

export function QuestionEditorPage() {
  const params = useParams<{ id?: string }>();
  const router = useRouter();
  const questionId = params.id === "new" ? undefined : params.id;
  const [form, setForm] = useState<QuestionInput>({ subjectId: "", subjectName: "", topicId: "", topicName: "", content: "", type: "SINGLE_CHOICE", difficulty: "MEDIUM", options: initialOptions, correctOptionIds: [], defaultPoints: 1, explanation: "" });
  const [subjects, setSubjects] = useState<Subject[]>([]);
  const [topics, setTopics] = useState<Topic[]>([]);
  const [loading, setLoading] = useState(Boolean(questionId));
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState("");

  useEffect(() => { void academicDataService.getTeacherAssignedClasses().then((classes) => { const items = [...new Map(classes.flatMap((item) => item.subjects).map((subject) => [subject.id, subject])).values()]; setSubjects(items.map((subject) => ({ ...subject, description: "", isActive: true }))); if (!questionId && items[0]) setForm((current) => ({ ...current, subjectId: items[0].id, subjectName: items[0].name })); }).catch((cause) => setError(cause instanceof Error ? cause.message : "Không thể tải môn học được phân công")); }, [questionId]);
  useEffect(() => { if (!form.subjectId) return; void academicDataService.getTopics(form.subjectId).then((items) => { setTopics(items); if (!questionId && items[0] && !form.topicId) setForm((current) => ({ ...current, topicId: items[0].id, topicName: items[0].name })); }).catch((cause) => setError(cause instanceof Error ? cause.message : "Không thể tải chủ đề")); }, [form.subjectId, form.topicId, questionId]);

  useEffect(() => { if (!questionId) return; void questionBankService.getQuestionById(questionId).then((question) => setForm({ subjectId: question.subjectId, subjectName: question.subjectName, topicId: question.topicId, topicName: question.topicName, content: question.content, type: question.type, difficulty: question.difficulty, options: question.options, correctOptionIds: question.correctOptionIds, defaultPoints: question.defaultPoints, explanation: question.explanation })).catch((cause) => setError(cause instanceof Error ? cause.message : "Không thể tải câu hỏi")).finally(() => setLoading(false)); }, [questionId]);

  function update<K extends keyof QuestionInput>(key: K, value: QuestionInput[K]) { setForm((current) => ({ ...current, [key]: value })); }
  function chooseSubject(subjectId: string) { const subject = subjects.find((item) => item.id === subjectId); if (!subject) return; setForm((current) => ({ ...current, subjectId: subject.id, subjectName: subject.name, topicId: "", topicName: "" })); }
  function chooseTopic(topicId: string) { const topic = topics.find((item) => item.id === topicId); if (!topic) return; setForm((current) => ({ ...current, topicId: topic.id, topicName: topic.name })); }
  function changeType(type: QuestionType) { update("type", type); if (type === "ESSAY") update("correctOptionIds", []); if (type === "TRUE_FALSE") update("options", [{ id: "true", label: "Đ", text: "Đúng" }, { id: "false", label: "S", text: "Sai" }]); if (type !== "TRUE_FALSE" && form.options.length < 4) update("options", initialOptions); }
  function updateOption(index: number, text: string) { setForm((current) => ({ ...current, options: current.options.map((option, optionIndex) => optionIndex === index ? { ...option, text } : option) })); }
  function toggleCorrect(id: string) { setForm((current) => { const multiple = current.type === "MULTIPLE_CHOICE"; const selected = current.correctOptionIds.includes(id); return { ...current, correctOptionIds: selected ? current.correctOptionIds.filter((item) => item !== id) : multiple ? [...current.correctOptionIds, id] : [id] }; }); }
  async function submit(event: React.FormEvent) { event.preventDefault(); setSaving(true); setError(""); try { const payload = { ...form, options: form.type === "ESSAY" ? [] : form.options, correctOptionIds: form.type === "ESSAY" ? [] : form.correctOptionIds }; if (!payload.content.trim()) throw new Error("Vui lòng nhập nội dung câu hỏi"); if (payload.type !== "ESSAY" && payload.correctOptionIds.length === 0) throw new Error("Vui lòng chọn đáp án đúng"); if (questionId) await questionBankService.updateQuestion(questionId, payload); else await questionBankService.createQuestion(payload); router.push("/teacher/question-bank"); } catch (cause) { setError(cause instanceof Error ? cause.message : "Không thể lưu câu hỏi"); } finally { setSaving(false); } }

  if (loading) return <AssessmentShell><LoadingPanel /></AssessmentShell>;
  const currentSubject = { topics };
  return <AssessmentShell><PageHeading eyebrow="Question bank" title={questionId ? "Chỉnh sửa câu hỏi" : "Tạo câu hỏi mới"} description="Câu hỏi sẽ được lưu vào Backend và có thể tái sử dụng khi tạo đề." action={<Button variant="ghost" onClick={() => router.push("/teacher/question-bank")}><ArrowLeft className="size-4" /> Quay lại</Button>} />
    {error ? <div className="mb-5"><ErrorPanel message={error} /></div> : null}
    <form onSubmit={(event) => void submit(event)} className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_330px]">
      <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-card sm:p-7"><div className="grid gap-5"><label className="grid gap-2 text-sm font-bold text-slate-700">Nội dung câu hỏi<textarea required value={form.content} onChange={(event) => update("content", event.target.value)} rows={5} placeholder="Nhập nội dung câu hỏi..." className="rounded-xl border border-slate-200 p-3 font-normal outline-none focus:border-brand-500 focus:ring-4 focus:ring-brand-50" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold text-slate-700">Loại câu hỏi<select value={form.type} onChange={(event) => changeType(event.target.value as QuestionType)} className="h-11 rounded-xl border border-slate-200 bg-white px-3 font-normal outline-none focus:border-brand-500">{[["SINGLE_CHOICE", "Trắc nghiệm một đáp án"], ["MULTIPLE_CHOICE", "Trắc nghiệm nhiều đáp án"], ["TRUE_FALSE", "Đúng / Sai"], ["ESSAY", "Tự luận"]].map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="grid gap-2 text-sm font-bold text-slate-700">Độ khó<select value={form.difficulty} onChange={(event) => update("difficulty", event.target.value as Difficulty)} className="h-11 rounded-xl border border-slate-200 bg-white px-3 font-normal outline-none focus:border-brand-500"><option value="EASY">Dễ</option><option value="MEDIUM">Trung bình</option><option value="HARD">Khó</option><option value="VERY_HARD">Rất khó</option></select></label></div>{form.type !== "ESSAY" ? <div className="rounded-2xl border border-slate-100 bg-slate-50/70 p-4"><div className="mb-3 flex items-center justify-between"><div><p className="text-sm font-extrabold text-slate-800">Danh sách đáp án</p><p className="mt-1 text-xs text-slate-500">Chọn đáp án đúng bằng nút tròn bên trái.</p></div>{form.type === "SINGLE_CHOICE" ? <span className="rounded-full bg-blue-50 px-2.5 py-1 text-[11px] font-bold text-blue-700">Chọn 1</span> : <span className="rounded-full bg-violet-50 px-2.5 py-1 text-[11px] font-bold text-violet-700">Chọn nhiều</span>}</div><div className="space-y-2">{form.options.map((option, index) => <div key={option.id} className="flex items-center gap-2"><button type="button" onClick={() => toggleCorrect(option.id)} className={`grid size-8 shrink-0 place-items-center rounded-lg border text-xs font-black ${form.correctOptionIds.includes(option.id) ? "border-brand-600 bg-brand-600 text-white" : "border-slate-300 bg-white text-slate-500"}`}>{option.label}</button><input value={option.text} onChange={(event) => updateOption(index, event.target.value)} required className="h-10 min-w-0 flex-1 rounded-lg border border-slate-200 bg-white px-3 text-sm outline-none focus:border-brand-500" placeholder={`Đáp án ${option.label}`} /></div>)}</div></div> : <label className="grid gap-2 text-sm font-bold text-slate-700">Hướng dẫn chấm / lời giải<textarea value={form.explanation} onChange={(event) => update("explanation", event.target.value)} rows={4} placeholder="Nhập gợi ý cho giáo viên chấm tự luận..." className="rounded-xl border border-slate-200 p-3 font-normal outline-none focus:border-brand-500" /></label>}<div className="flex justify-end gap-3 border-t border-slate-100 pt-5"><Button type="button" variant="outline" onClick={() => router.push("/teacher/question-bank")}>Hủy</Button><Button type="submit" disabled={saving}><Save className="size-4" />{saving ? "Đang lưu..." : "Lưu câu hỏi"}</Button></div></div></section>
      <aside className="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-card"><h2 className="font-extrabold text-slate-900">Phân loại</h2><p className="mt-1 text-xs text-slate-500">Giúp tìm và chọn câu hỏi nhanh hơn khi tạo đề.</p><div className="mt-5 grid gap-4"><label className="grid gap-2 text-sm font-bold text-slate-700">Môn học<select value={form.subjectId} onChange={(event) => chooseSubject(event.target.value)} className="h-11 rounded-xl border border-slate-200 bg-white px-3 font-normal outline-none focus:border-brand-500">{subjects.map((subject) => <option key={subject.id} value={subject.id}>{subject.name}</option>)}</select></label><label className="grid gap-2 text-sm font-bold text-slate-700">Chủ đề<select value={form.topicId} onChange={(event) => chooseTopic(event.target.value)} className="h-11 rounded-xl border border-slate-200 bg-white px-3 font-normal outline-none focus:border-brand-500">{currentSubject.topics.map((topic) => <option key={topic.id} value={topic.id}>{topic.name}</option>)}</select></label><label className="grid gap-2 text-sm font-bold text-slate-700">Điểm mặc định<input type="number" min="0" step="0.25" value={form.defaultPoints} onChange={(event) => update("defaultPoints", Number(event.target.value))} className="h-11 rounded-xl border border-slate-200 px-3 font-normal outline-none focus:border-brand-500" /></label></div></aside>
    </form>
  </AssessmentShell>;
}
