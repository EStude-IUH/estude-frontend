"use client";

import { useMemo, useState } from "react";
import Link from "next/link";
import { ArrowLeft, BookOpen, ChevronRight, Search } from "lucide-react";
import { BrandLogo } from "@/components/brand-logo";
import { Input } from "@/components/ui/form-control";

const guides = [
  { role: "Teacher", label: "Giáo viên", articles: ["Điểm danh lớp học", "Tạo và cấu hình bài thi", "Thiết lập mật khẩu bài thi", "Chuyển câu hỏi thành ảnh", "Gửi thông báo"] },
  { role: "Student", label: "Học sinh", articles: ["Nhận thông báo", "Xem lịch sử điểm danh", "Tham gia bài thi", "Nhập mật khẩu", "Zoom ảnh câu hỏi"] },
  { role: "Parent", label: "Phụ huynh", articles: ["Sử dụng dashboard", "Xem thông báo", "Theo dõi điểm danh", "Xem bài thi sắp tới"] },
  { role: "Admin", label: "Quản trị viên", articles: ["Dashboard quản trị", "Gửi thông báo hệ thống", "Quản lý người dùng và phân quyền"] },
];

export function HelpCenter() {
  const [query, setQuery] = useState("");
  const [active, setActive] = useState(guides[0].articles[0]);
  const filtered = useMemo(() => guides.map((guide) => ({ ...guide, articles: guide.articles.filter((article) => `${guide.label} ${article}`.toLocaleLowerCase("vi").includes(query.toLocaleLowerCase("vi"))) })).filter((guide) => guide.articles.length), [query]);
  const activeRole = guides.find((guide) => guide.articles.includes(active));
  return <main className="min-h-screen bg-slate-50"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center px-4 sm:px-6"><Link href="/about"><BrandLogo withShadow={false} /></Link><Link href="/login" className="ml-auto inline-flex items-center gap-2 rounded-lg px-3 py-2 text-sm font-bold text-brand-700 hover:bg-blue-50"><ArrowLeft className="size-4" /> Về đăng nhập</Link></div></header><div className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><div><p className="text-xs font-black uppercase tracking-[.18em] text-brand-600">Help center</p><h1 className="mt-2 text-3xl font-black tracking-tight sm:text-4xl">Hướng dẫn sử dụng EStude</h1><p className="mt-2 text-slate-500">Tìm hướng dẫn ngắn gọn theo đúng vai trò của bạn.</p></div><div className="mt-6 max-w-xl"><Input icon={Search} value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Tìm: điểm danh, mật khẩu, thông báo..." aria-label="Tìm hướng dẫn" /></div><div className="mt-8 grid gap-6 lg:grid-cols-[280px_minmax(0,1fr)]"><aside className="hidden h-fit rounded-2xl border border-slate-200 bg-white p-3 shadow-card lg:block" aria-label="Danh mục hướng dẫn">{filtered.map((guide) => <div key={guide.role} className="mb-3 last:mb-0"><p className="px-3 py-2 text-xs font-black uppercase tracking-wide text-slate-400">{guide.label}</p>{guide.articles.map((article) => <button type="button" key={article} onClick={() => setActive(article)} className={`flex w-full items-center justify-between rounded-xl px-3 py-2.5 text-left text-sm font-semibold ${active === article ? "bg-brand-50 text-brand-700" : "text-slate-600 hover:bg-slate-50"}`}>{article}<ChevronRight className="size-4" /></button>)}</div>)}</aside><div className="space-y-3 lg:hidden">{filtered.map((guide) => <details key={guide.role} className="rounded-2xl border border-slate-200 bg-white p-4" open={guide.articles.includes(active)}><summary className="cursor-pointer font-extrabold">{guide.label}</summary><div className="mt-3 space-y-1">{guide.articles.map((article) => <button type="button" key={article} onClick={() => setActive(article)} className={`block min-h-11 w-full rounded-xl px-3 py-2 text-left text-sm font-semibold ${active === article ? "bg-brand-50 text-brand-700" : "text-slate-600"}`}>{article}</button>)}</div></details>)}</div><article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-card sm:p-8"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-blue-50 text-brand-700"><BookOpen className="size-5" /></span><div><p className="text-xs font-bold text-brand-600">{activeRole?.label ?? "Hướng dẫn"}</p><h2 className="text-2xl font-black">{active}</h2></div></div><div className="mt-7 space-y-5 text-sm leading-7 text-slate-600"><p>Chọn chức năng tương ứng trong thanh điều hướng. Hệ thống sẽ chỉ hiển thị dữ liệu và thao tác mà tài khoản của bạn được cấp quyền.</p><ol className="list-decimal space-y-3 pl-5"><li>Kiểm tra lớp, môn học hoặc đối tượng đang chọn.</li><li>Nhập thông tin bắt buộc; lỗi sẽ hiển thị ngay tại bước hiện tại.</li><li>Xem lại trạng thái và xác nhận thao tác quan trọng trước khi gửi.</li></ol><p className="rounded-xl bg-blue-50 p-4 text-brand-800">Nếu không thấy một chức năng, hãy liên hệ quản trị viên để kiểm tra vai trò và nhóm quyền của tài khoản.</p></div></article></div></div></main>;
}
